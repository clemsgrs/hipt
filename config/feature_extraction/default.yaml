region_dir: '/home/user/code/git/clemsgrs/hs2p/output/tcga_brca_survival_debug/patches/'

output_dir: 'output'
experiment_name: 'tcga_brca_survival_debug'
resume: False

slide_list:

region_size: 4096
format: 'jpg'
level: 'global'

pretrain_256: 'checkpoints/vit_256_small_dino.pth'
pretrain_4096: 'checkpoints/vit_4096_xs_dino.pth'

wandb:
  enable: True
  project: 'hipt'
  username: 'clemsg'
  exp_name: '${experiment_name}'
  dir: '/home/user/'
  group:
